{
  "name": "@jdc/database",
  "version": "0.0.1",
  "description": "Prisma 创建本地开发数据库",
  "main": "index.ts",
  "scripts": {
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "db:generate": "prisma generate",
    "db:push": "prisma db push --skip-generate"
  },
  "keywords": [
    "prisma"
  ],
  "dependencies": {
    "prisma": "^4.8.1"
  },
  "devDependencies": {
    "@prisma/client": "^4.8.1"
  }
}
